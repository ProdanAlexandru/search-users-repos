import React from 'react'
import { Typography } from '@material-ui/core'

const Title = () => {
    return (
        <div className="title-repos">
            <Typography className="titleName" variant= "h5" color="inherit">SEARCH REPOSITORIES</Typography>
        </div>
    )
}

export default Title
